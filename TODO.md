# TODO

## 功能待办

- [x] OSS 支持
- [x] 前端播放器组件
- [x] 支持：提供PPT文件，提取视觉风格，加入ppt风格模版
- [ ] 增加一个种子数据workspace，新建账号时，自动初始化到对应的用户下（包括历史会话记录、task产物等等），可以自行删除。
- [ ] 播放组件优化：增加字幕（字幕需要切分并有timeline）。每页PPT间隔太短，体验不好，应该增加至少1s的空间隔（可以是工程上实现，在前端增加播放间隔）
- [ ] 导出PPTX
- [ ] 增加图片插入能力
- [ ] 导出视频文件
- [ ] 系统主题明确：「墨纸杂志」、「瑞士国际主义」、「奶油粗野主义」

## BUG
- [ ] 表单取消后，会再次出现，无法取消。只有刷新页面，才会正常消失。

## 边界限制

- [ ] 每个用户限制5个工作区
- [ ] 每个工作区限制5个文档。
- [ ] 每个工作区限制生成10个PPT文件、10个自定义风格任务
- [ ] 每个PPT文件下限制生成2个口播稿
- [ ] 每个用户限制5个自定义风格 

超出限制根据场景提示用户删除不再需要的任务或者文档。

## 体验问题

1. [x] 历史会话加载后，滚动条应在底部，而非停在中间
2. [x] 历史消息向上滚动加载过多时卡顿（考虑 tool_result 延迟加载：仅在点击查看详情时按 message_id 定向获取）
3. [ ] 新产出完成时增加气泡提示（如"xxx 生成完成，可预览/编辑/生成口播稿"）
4. [ ] 发送新消息后，自动滚动将用户消息滚到视窗顶部
5. [x] 流式消息渲染卡住后突然全部加载，非逐字流式
6. [ ] 引用角标渲染不稳定，有时无法显示章节-标题-页；需考虑 fallback（文档可能无标题/切分缺少标志）
7. [x] 表单能力增强：单选默认选中、多选默认勾选等（recommended 字段自动预选 + 推荐标记）
8. [x] 表单能力增强：ppt用户澄清的问题，需要优化（重写为 Topic/Purpose/Length/Sources 4 问，内联 chips 布局，支持自定义输入）
9. [x] PPT 技能表单在已有上下文时，会将上下文内容默认当作主题，跳过了主题选择步骤：主题相关的内容作为用户偏好放在system prompt中，变更主题立即变更system prompt。
10. [x] 预览HTML列表显示效果问题（PNG方案ok但是对自定义风格不友好。HTML方案在列表显示效果不好）：当前改为系统模版使用png，用户生成的使用html；

## 效果评估
1. [ ] PPT SKILL的产物效果；
2. [ ] PPT SKILL对于用户自定义模版的产物效果、还原度效果等；
3. [ ] PPTX风格提取效果；
4. [ ] TTS优化：当前TTS使用qwen3-tts-flash，中英掺杂场景效果不好，英文读取来很难受，时不时还冒出来日语。考虑切换cosyvoice-v3-flash，稳定性很好，但是生成耗时是前者的2倍多，成本也更高。

## 稳定性
- [ ] 断线重连机制

## 架构与决策

1. [ ] 上下文管理优化（token 成本、注意力效果、压缩频率之间的权衡）
2. [ ] 消息渲染方案（历史消息 + 实时 SSE 合并渲染）
3. [ ] 清晰架构（通用 Agent + SKILL 注入业务 + 定制 Tools，业务与 Agent 能力分离）
4. [ ] 会话消息加载性能优化（tool_result 延迟加载）
5. [ ] LangGraph 能力边界评估（提供了什么？缺什么？）

### 上下文管理优化

> 当前压缩太频繁，会话内容较多时几乎每次用户消息都会触发 summary。

- [ ] System Prompt 永不压缩
- [ ] 工具结果在压缩时清空
    - ppt技能和reference很占用上下文，在ppt生成完成闭环后，应该将load_skill中的输出清空或替换为「再次执行工具读取」等提示。
    - save_ppt：可清空输出的文件内容或替换为其他总结。
    - save_narration：可清空或替换为其他总结。
- [ ] UI 优化：上下文压缩存在卡顿。应该有提示或者移步压缩？
