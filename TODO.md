# TODO

## 功能待办

- [ ] OSS 支持：产出存储到 OSS，点击文件直接访问公开链接
- [ ] 工具管理：异常处理与容错机制
- [x] 前端播放器组件
- [ ] 导出视频文件
- [x] 支持：提供PPT文件，提取视觉风格，加入ppt风格模版
- [ ] 增加一个种子数据workspace，新建账号时，自动初始化到对应的用户下（包括历史会话记录、task产物等等），可以自行删除。
- [ ] 播放组件优化：增加字幕（字幕需要切分并有timeline）。每页PPT间隔太短，体验不好，应该增加至少1s的空间隔（可以是工程上实现，在前端增加播放间隔）

## 选型

- [ ] 当前TTS使用qwen3-tts-flash，中英掺杂场景不好，英文读取来很难受，时不时还冒出来日语。需要切换cosyvoice-v3-flash，实测稳定性很好，但是生成耗时是前者的2倍多，成本也更高。

## 边界限制

- [ ] 每个用户限制5个工作区
- [ ] 每个工作区限制5个文档。
- [ ] 每个工作区限制生成10个PPT文件、10个自定义风格任务
- [ ] 每个PPT文件下限制生成2个口播稿
- [ ] 每个用户限制5个自定义风格 

超出限制根据场景提示用户删除不再需要的任务或者文档。

## 上下文管理优化

> 当前压缩太频繁，会话内容较多时几乎每次用户消息都会触发 summary。

- [ ] System Prompt 永不压缩
- [ ] PPT 文件内容用完即清空（产出完成后）
- [ ] 工具结果在压缩时清空
- [ ] 保存产出后，将工具调用入参 content 从上下文移除，替换为文件引用；并提供读取产出内容的工具
- [ ] UI 优化

## 体验问题

1. [x] 历史会话加载后，滚动条应在底部，而非停在中间
2. [x] 历史消息向上滚动加载过多时卡顿（考虑 tool_result 延迟加载：仅在点击查看详情时按 message_id 定向获取）
3. [ ] 新产出完成时增加气泡提示（如"xxx 生成完成，可预览/编辑/生成口播稿"）
4. [ ] 发送新消息后，自动滚动将用户消息滚到视窗顶部
5. [ ] 流式消息渲染卡住后突然全部加载，非逐字流式
6. [ ] 引用角标渲染不稳定，有时无法显示章节-标题-页；需考虑 fallback（文档可能无标题/切分缺少标志）
7. [ ] 表单能力增强：单选默认选中、多选默认勾选等
8. [ ] 表单能力增强：ppt用户澄清的问题，需要优化，比如用户多个文档相关性不高，要生成ppt，没有提供任何主题，澄清问题中需要询问主题
9. [x] PPT 技能表单在已有上下文时，会将上下文内容默认当作主题，跳过了主题选择步骤：主题相关的内容作为用户偏好放在system prompt中，变更主题立即变更system prompt。
10. [ ] 预览HTML列表显示效果问题（PNG方案ok但是对自定义风格不友好。HTML方案在列表显示效果不好）

## 稳定性
- [ ] 断线重连机制

## 架构与决策

1. [ ] 上下文压缩策略（token 成本、注意力效果、压缩频率之间的权衡）
2. [ ] 消息渲染方案（历史消息 + 实时 SSE 合并渲染）
3. [ ] 清晰架构（通用 Agent + SKILL 注入业务 + 定制 Tools，业务与 Agent 能力分离）
4. [ ] 会话消息加载性能优化（tool_result 延迟加载）
5. [ ] LangGraph 能力边界评估（提供了什么？缺什么？）
