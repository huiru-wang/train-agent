

1. OSS支持：产出存储到OSS上，在产出中点击对应的文件直接访问OSS公开链接。
2. A2UI：风格预览文件不应该是产出，而是中间步骤。
3. 上下文管理
- SystemPrompt永不压缩；
- PPT文件内容：用完即清空；（产出完成）
- 工具结果，压缩时清空；

4. 工具管理
5. /Users/whr/workspace/projects/train-agent 为什么使用了SummarizationMiddleware之后，当聊天内容上下文增多，触发了压缩，在用户视角里的历史记录中，出现了压缩内容，在用户视角应该永远看到的是实际历史消息，而不是agent上下文吧。查看具体的代码，是什么原因。确认完整的历史记录仍然存在，只是前端的hook获取的不是完整的历史记录，还是有别的原因？
6. checkpoint做了什么？如何工作的


[] OSS支持：产出存储到OSS上，在产出中点击对应的文件直接访问OSS公开链接。
[] 保存产出后，将工具调用的入参content，从上下文移除，替换为文件引用。
[] 集成Store
[] 上下文管理
- SystemPrompt永不压缩；
- PPT文件内容：用完即清空；（产出完成）
- 工具结果，压缩时清空；
[] 工具管理：异常处理；