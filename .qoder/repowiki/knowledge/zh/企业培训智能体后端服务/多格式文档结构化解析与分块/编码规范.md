- 所有解析器类均提供统一的 `parse` 方法作为入口，返回 `list[DocumentSection]` 类型，保持接口一致性。
- 在提取章节内容时，均维护 `chapter_title` 变量以追踪当前一级标题，并将其赋值给子章节的 `parent_title` 字段，以保留文档层级关系。
- 当文档中未检测到任何有效标题结构时，所有解析器均提供降级策略（Fallback），将整个文档或页面作为单个 Section 返回，避免空结果。