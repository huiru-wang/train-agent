- 所有数据库查询方法均采用异步模式 (`async/await`)，并在执行前通过 `ensure_initialized` 确保连接已建立。
- 复杂对象（如消息内容、工具调用参数、扩展配置）在存入 SQLite 前统一序列化为 JSON 字符串，读取时反序列化并提供默认值容错。
- 存储操作接口普遍以 `workspace_id` 作为核心隔离维度，确保不同工作空间的数据、文件及向量集合相互独立。