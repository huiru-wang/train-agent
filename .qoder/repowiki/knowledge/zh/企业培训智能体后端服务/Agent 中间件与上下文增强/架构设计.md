- **工厂模式入口**：通过 `create_middlewares` 统一组装并返回中间件列表，依赖 `AppContext` 注入数据库连接。
- **分层处理逻辑**：包含生命周期日志（`logging_middlewares`）、状态持久化（`MessageHistoryMiddleware`）、模型请求清洗（`model_message_sanitizer`）以及动态上下文增强（`inject_doc_context`）。
- **自定义摘要策略**：继承 `SummarizationMiddleware` 实现 `TrainAgentSummarizationMiddleware`，引入冷却机制防止频繁触发摘要。