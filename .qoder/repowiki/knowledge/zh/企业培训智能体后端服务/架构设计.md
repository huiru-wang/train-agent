- **启动编排**：在 `src/api/routes.py` 的 `startup` 事件中初始化数据库，并通过 `src/agent/graph.py` 的 `_make_default_graph` 集中实例化所有核心组件（DB, VectorStore, FileStore, SkillManager）。
- **依赖注入**：采用工厂模式将底层存储与解析服务注入到 Agent 图与 API 路由中，实现业务逻辑与基础设施的解耦。
- **中间件链**：在 Agent 执行链路中串联日志记录、上下文注入、消息清洗及对话摘要等中间件，形成统一的请求处理生命周期。