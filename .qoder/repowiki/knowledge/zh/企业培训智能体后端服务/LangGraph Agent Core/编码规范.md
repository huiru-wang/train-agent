- 中间件类统一继承自 `AgentMiddleware` 或 `SummarizationMiddleware`，并实现 `abefore_agent` / `aafter_agent` 等异步钩子方法。
- 工具创建采用工厂函数模式（如 `create_rag_search_tool`），在 `graph.py` 中统一实例化并注入依赖。
- 消息历史记录通过 `additional_kwargs` 中的 `lc_source` 标记来区分普通消息与系统生成的摘要消息，避免重复持久化。