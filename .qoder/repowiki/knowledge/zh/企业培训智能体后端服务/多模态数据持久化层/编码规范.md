- 所有数据库写操作在执行后均显式调用 await self.connection.commit() 确保事务提交。
- 向量存储操作在写入或检索前通过 _get_collection 或 _get_existing_collection 动态获取或创建以 workspace_id 命名的集合。
- 文件存储路径统一遵循 base_dir / workspace_id / filename 的层级结构，并在写入前自动创建父目录。