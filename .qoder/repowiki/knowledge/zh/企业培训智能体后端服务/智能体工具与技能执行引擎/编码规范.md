- 采用工厂模式创建工具（如 `create_rag_search_tool`），通过闭包注入外部依赖（如 `VectorStore`、`SkillManager`），实现工具逻辑与基础设施的解耦。
- 所有工具函数的返回值均序列化为 JSON 字符串（`json.dumps`），确保 Agent 能够以统一的结构化格式解析执行结果或错误信息。
- 在执行外部脚本或命令时，强制实施超时控制（`asyncio.wait_for`）和输出长度截断，防止因长时间运行或海量输出导致智能体上下文溢出。
- 涉及文件系统操作的工具均包含严格的安全校验逻辑，例如在 `run_skill_script.py` 中通过 `resolve()` 和 `parents` 检查确保脚本路径未越出指定的技能目录。