- 采用 Bash Shell 作为主要编排语言，通过 `set -euo pipefail` 确保执行安全。
- 核心生命周期由 `start.sh`（后台启动后端 API、LangGraph 及前端）、`stop.sh`（基于端口占用检测的优雅停机）和 `restart.sh`（组合调用）构成。
- `doctor.sh` 负责前置依赖检查，涵盖工具链（uv, node, pnpm/npm）、项目配置文件及端口冲突检测。
- 集成 Perl 单行脚本 `utc2local.pl` 用于处理日志中的 UTC 时间戳转换，适配本地时区查看需求。