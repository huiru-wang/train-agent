- 所有 Shell 脚本均声明 `#!/usr/bin/env bash` 并开启严格错误处理模式 `set -euo pipefail`。
- 统一通过 `$(cd "$(dirname "$0")/.." && pwd)` 动态计算项目根目录，确保脚本在任意路径下均可正确引用资源。
- 服务启停逻辑强依赖于固定端口（8000, 2024, 3000）的检测，并使用 `logs/` 目录下的 `.pid` 和 `.log` 文件进行状态管理与故障排查。