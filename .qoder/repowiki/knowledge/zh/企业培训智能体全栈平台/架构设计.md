- **双进程后端架构**：FastAPI (:8000) 负责 CRUD 业务逻辑，LangGraph (:2024) 独立运行流式智能体，两者通过 SQLite、ChromaDB 和文件系统实现数据同步。
- **前后端通信契约**：前端通过 `frontend/src/lib/api.ts` 封装的 REST 客户端与后端交互，并利用 `@langchain/react` 直接连接 LangGraph 端口获取流式对话状态。
- **统一运维编排**：根目录下的 `scripts/` 提供 `start.sh`、`dev.sh` 等脚本，统一管理三个服务进程的启停、健康检查及日志监控。