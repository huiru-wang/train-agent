- 工作区隔离：所有数据（文件、向量 Collection、数据库记录）均以 workspace_id 为维度进行物理或逻辑隔离。
- 异步文档处理：文档上传接口立即返回，解析、分块、索引和摘要生成通过 BackgroundTasks 在后台异步执行并更新状态。
- 渐进式技能披露：Agent 启动时仅加载技能名称与描述，仅在需要时通过 load_skill 工具动态加载完整技能内容以节省 Token。
- 客户端状态轮询：前端对文档处理状态（1.5s 间隔）和任务产出列表（5s 间隔）采用定时轮询机制而非 WebSocket 推送。