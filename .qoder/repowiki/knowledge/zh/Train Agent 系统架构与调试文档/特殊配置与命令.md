- **日志排查**：日志位于 `logs/` 目录（backend.log, frontend.log, langgraph.log），每次重启清空。
- **调试产物**：统一存放于 `debug/{workspace_id}/` 目录。
- **Browser-use 调试**：使用 `browser-use open/screenshot/eval` 进行前端交互排查，支持注入 `window.fetch` 拦截器监控网络请求。