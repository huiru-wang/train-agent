- 所有涉及客户端交互或状态的页面组件均使用 "use client" 指令标记。
- API 请求统一通过 `src/lib/api.ts` 中的封装函数发起，并集中处理错误逻辑。
- 业务组件按功能模块（chat, document, task, workspace）在 `src/components` 下分目录组织。
- 页面级组件负责数据获取与布局编排，将具体业务逻辑下沉至 `components` 目录下的专用面板组件。